on:
  release:
    types: [published]

jobs:
  refresh_presentation_branch:
    uses: nolte/gh-plumbing/.github/workflows/reuseable-release-cd-refresh-master.yml@v1.0.19
    with:
      target_branch: main
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
