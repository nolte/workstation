on:
  release:
    types: [published]

jobs:
  refresh_presentation_branch:
    uses: nolte/gh-plumbing/.github/workflows/reusable-release-cd-refresh-master.yml@v2.0.0
    with:
      target_branch: main
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
